/*2. Se requiere un algoritmo para obtener la suma de diez cantidades mediante la utilización de un ciclo for.*/

/*sum = 0;

for (let i = 0; i < 10; i++) {
    value = prompt('Insert value');
    sum = sum + value;
}

document.write(sum);*/

/*3. Se requiere un algoritmo para obtener
 la estatura promedio de un grupo de personas, 
cuyo número de miembros se desconoce, el ciclo debe 
efectuarse siempre y cuando se tenga una estatura registrada.*/

/*sumHeights= 0;
counter= 0;
keep = "s";
averageHeights = 0;

keep = prompt('Write s/n')

while (keep == "s") {
    heights= parseFloat(prompt("Your Height:"));
    sumHeights= sumHeights + heights;
    counter= counter + 1;
    keep = prompt("Do you want keep s/n:");
    averageHeights= sumHeights / counter;
    document.write("su promedio es :" + averageHeights);
}*/

/*4. Se requiere un algoritmo para determinar, de N cantidades, cuántas son menores o iguales a cero y cuántas mayores a cero.*/
/*n=Number(prompt("Insert numbers"));
n1=0;
n2=0;
n3=0;
for (let i = 0; i < n; i++) {
    numbers=Number(prompt(''));
    
    if (numbers == 0) {
        n1=n1+1;    
    }else if (numbers < 0) {
        n2=n2+1;  
    } else {
        n3=n3+1;  
    }

   document.write("equals to 0:" + n1);
   document.write("min to 0:" + n2);
   document.write("max to 0:" + n3); 
}*/

/*5. Realice un algoritmo para generar e imprimir 
los números pares e impares que se encuentran
 entre 0 y 100. Además muestre la multiplicación de todos estos.*/

 






